package com.testNG;

public class TestNg_Parameters {
	
	/*
	 * we can use parameters for the adding global variables to program.
	 * Global variables are defined on testng.xml file
	 * but in framework we usig prop file to add the global variable
	 * 
	 */

}
